<idea-plugin>
    <id>de.moritzf.openai-usage-quota</id>
    <name>LLM Subscription Usage</name>
    <vendor>Moritzf</vendor>

    <description><![CDATA[
        <p>Track and use your LLM subscriptions directly in the IDE.</p>
        <ul>
            <li><b>Quota at a glance</b> — usage for OpenAI (ChatGPT/Codex), Claude, SuperGrok/xAI, GitHub Copilot, Cursor, OpenCode Go, Ollama Cloud, Z.ai, MiniMax, Mistral, and Kimi in the status bar or main toolbar, with a detailed popup showing usage windows and reset times.</li>
            <li><b>MCP tools</b> — quota lookup, subscription-backed web search (OpenAI/Codex, SuperGrok/xAI, Mistral, Kimi, Z.ai, MiniMax, Ollama), image generation (OpenAI/Codex, SuperGrok/xAI Imagine, Mistral, Z.ai GLM-Image, MiniMax) with optional file output, video generation (SuperGrok/xAI Imagine, Z.ai CogVideoX), document-to-markdown (Mistral OCR, Z.ai GLM-OCR, OpenAI/Codex, SuperGrok), speech-to-text (OpenAI/Codex, SuperGrok/xAI, Mistral, Z.ai), and text-to-speech (OpenAI/Codex, SuperGrok/xAI, Mistral, MiniMax). Served through IntelliJ's built-in MCP server to the IDE's AI chat and to any external MCP-capable agent.</li>
            <li><b>Local OpenAI-compatible proxy</b> — expose selected subscriptions (OpenAI/Codex, SuperGrok, GitHub Copilot, Kimi, MiniMax, Mistral, Ollama, OpenCode Zen, Z.ai) through standard OpenAI/LiteLLM endpoints, so tools like JetBrains Junie can use them as a custom LLM provider. Compatibility is tested with the Junie CLI and JetBrains' API Connections for AI features such as the integrated chat.</li>
            <li><b>MCP server URL sync</b> — keeps JSON, TOML, and YAML client config files pointed at IntelliJ's active MCP endpoint when its port changes.</li>
        </ul>
        <p>All credentials are stored in IntelliJ Password Safe.</p>
    ]]></description>

    <depends>com.intellij.modules.platform</depends>
    <depends optional="true" config-file="openai-usage-quota-with-mcp.xml">com.intellij.mcpServer</depends>

    <resource-bundle>messages.OpenAiUsageQuotaBundle</resource-bundle>

    <extensions defaultExtensionNs="com.intellij">
        <applicationConfigurable
                id="openai.usage.quota.settings"
                key="settings.displayName"
                bundle="messages.OpenAiUsageQuotaBundle"
                parentId="tools"
                instance="de.moritzf.quota.idea.settings.QuotaSettingsConfigurable"/>
        <postStartupActivity implementation="de.moritzf.quota.idea.mcp.McpServerUrlSyncStartupActivity"/>
        <postStartupActivity implementation="de.moritzf.quota.idea.openai.OpenAiProxyStartupActivity"/>
        <statusBarWidgetFactory implementation="de.moritzf.quota.idea.ui.toolbar.QuotaStatusBarWidgetFactory" id="openai.usage.quota.widget"/>
    </extensions>

    <actions>
        <action id="openai.usage.quota.mainToolbar"
                class="de.moritzf.quota.idea.ui.toolbar.QuotaMainToolbarAction">
            <add-to-group group-id="MainToolbarRight"/>
        </action>
    </actions>
</idea-plugin>
